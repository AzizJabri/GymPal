import { ExerciseNamePipe } from './exercise-name.pipe';

describe('ExerciseNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ExerciseNamePipe();
    expect(pipe).toBeTruthy();
  });
});
